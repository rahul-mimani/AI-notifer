Replace the entire contents of src/extension.ts with a minimal VS Code extension registering one command ai-change-impact-notifier.smokeTest. The command should: select a Copilot chat model via vscode.lm.selectChatModels({ vendor: 'copilot' }), send a single user message asking the model to reply with exactly the word "pong", stream the response by iterating for await (const chunk of request.text), and show the concatenated response in an information message. Show an error message if no Copilot model is available or if the API call throws. Use the exact API: vscode.LanguageModelChatMessage.User(text) for the message, model.sendRequest(messages, {}, cancellationToken) for the call. Do not invent alternative API surfaces. Also update package.json to register the command with title "AI Impact: Smoke Test" under contributes.commands and add onCommand:ai-change-impact-notifier.smokeTest to activationEvents.